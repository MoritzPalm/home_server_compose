services:
  vikunja:
    image: vikunja/vikunja
    container_name: vikunja
    environment:
      VIKUNJA_SERVICE_PUBLICURL: http://${HOST_IP}:3456
      VIKUNJA_DATABASE_HOST: db
      VIKUNJA_DATABASE_PASSWORD: ${VIKUNJA_POSTGRES_PASSWORD}
      VIKUNJA_DATABASE_TYPE: postgres
      VIKUNJA_DATABASE_USER: vikunja
      VIKUNJA_DATABASE_DATABASE: vikunja
      VIKUNJA_SERVICE_JWTSECRET: ${VIKUNJA_JWT_SECRET}
    ports:
      - 3456:3456
    volumes:
      - ${COMPOSE_ROOT}/vikunja/files:/app/vikunja/files
    security_opt:
      - no-new-privileges:true
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    labels:
      - "homepage.group=Productivity"
      - "homepage.name=Vikunja"
      - "homepage.icon=vikunja"
      - "homepage.href=http://${HOST_IP}:3456"
      - "homepage.description=Task manager"
  db:
    image: postgres:17
    container_name: vikunja_db
    environment:
      POSTGRES_PASSWORD: ${VIKUNJA_POSTGRES_PASSWORD}
      POSTGRES_USER: vikunja
    volumes:
      - ${DATABASE_DIR}/vikunja:/var/lib/postgresql
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h localhost -U $$POSTGRES_USER"]
      interval: 2s
      start_period: 30s
